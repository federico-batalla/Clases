/*//------------Funciones-----------------

function incializarArreglo(arreglo: number[]): void {
  for (let indice = 0; indice < arreglo.length; indice++) {
    arreglo[indice] = Number(prompt("ingrese numero"));
  }
}
//-------------------------------------------
function mostrarArreglosInvertidos(arreglo: number[]): void {
  for (let indice: number = arreglo.length - 1; indice >= 0; indice--) {
    console.log(arreglo[indice]);
  }
}

//----------------principal--------------
let numeros3: number[] = new Array(6);

incializarArreglo(numeros3);
mostrarArreglosInvertidos(numeros3);
*/
